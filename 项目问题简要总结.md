# 项目问题简要总结

## 1. 是否有地图

有，但不是 SLAM/栅格/导航栈地图。

项目里使用的是一套手工硬编码的 AprilTag 拓扑地图：

- `task.csv`：定义任务路径，也就是车辆应该经过哪些 Tag。
- `trajectory_waypoint.py`：定义 `(当前Tag, 下一个Tag)` 对应的局部 waypoint。
- `signal_phases.py`：定义 `(当前Tag, 下一个Tag)` 属于哪个交通灯相位。

小车平时靠车道线行驶；到停止线/路口时，检测当前 Tag，查下一个目标 Tag，再执行对应的局部轨迹。

## 2. 确定下一个目标 Tag 后会做什么

流程：
1. 检测当前 Tag。
2. 根据任务路径得到 `next_id`。
3. 遇到停止线后停车。
4. 查询 `(current_tag, next_tag)` 对应的交通灯相位。
5. 如果当前 `/green_phases` 允许通行，就发布 `start_end_id = [current_tag, next_tag]`。
6. `traj_follow_node.py` 根据 `trajectory_waypoint.py` 查 waypoint。
7. 小车执行局部轨迹。
8. 轨迹完成后恢复车道跟随。

## 3. “硬编码右转”是什么意思

不是运行时判断“目标 Tag 在右边所以右转”。

而是开发时已经知道从某个 Tag 到另一个 Tag 的道路几何关系，于是提前写好：

```python
(current_tag, next_tag): [(x, y, theta), ...]
```

运行时只查表执行。例如某条边需要右转，就把右转所需的局部 waypoint 写死在表里。

## 4. 如何启动让小车按地图运动

需要 PC 端和 PI 端一起运行。

PC 端至少启动：

```bash
roscore
roslaunch vpa_trafficsignal task_assign.launch
roslaunch vpa_trafficsignal traffic_signal.launch
```

PI 端启动：

```bash
export robot_name=fiona
roslaunch vpa_robot_operation start_operation.launch freeflow_speed:=0.2
```

还要放开刹车：

```bash
rostopic pub -r 5 /global_brake std_msgs/Bool "data: false"
rostopic pub -r 5 /fiona/local_brake std_msgs/Bool "data: false"
```

`fiona` 替换成实际机器人名。

## 5. 完整赛道 GUI 是什么

指 PC 侧 `vpa_duckierace` 包里的赛道监控和区域管理系统。

主要功能：

- 显示机器人电量、速度、刹车状态、圈数等。
- 通过俯视摄像头检测车顶 AprilTag。
- 管理合流区、补能区、充电门、终点线。
- 发布 `/global_brake` 或 `/<robot_name>/local_brake`。

它不是小车按 Tag 地图运动的核心必要条件，但可能参与比赛/实验的区域管控。

## 6. 是否有超车逻辑

没有真正的自动超车逻辑。

项目里有：

- 前车检测；
- ACC 自适应跟车；
- 合流区让行；
- 手动调速/手动切换跟踪线颜色。

但没有：

- 自动变道；
- 判断旁车道是否安全；
- 绕过前车；
- 超车后回原车道；
- `OVERTAKING` 状态机。

所以它会跟车、减速、停车，但不会自动超车。

## 7. 自适应跟车是什么意思

自适应跟车就是根据前车距离自动调节速度。

- 前方没车：按正常速度走。
- 前车变近：减速。
- 前车很近或停止：停车。
- 前车重新移动：等待一段时间后继续跟上。

方向仍由车道跟随或轨迹跟踪决定；ACC 主要限制线速度。

## 8. 如何判断前面有没有车

主线使用：

- 前向 ToF 距离传感器；
- 摄像头 AprilTag 检测。

流程：

1. `tof_driver_i2c.py` 发布 `front_range` 和 `front_range_status`。
2. `acc_operation_enhanced.py` 判断 ToF 是否有效，且距离是否小于阈值。
3. 如果 ToF 检测到近距离物体，再调用 `FrontCarDetectorEnhanced`。
4. `FrontCarDetectorEnhanced` 在图像中寻找 ID 为 `100-199` 的 AprilTag。
5. ToF 有近距离物体且视觉检测到前车 Tag，才认为前面有车。
6. 发布 `perception/lead_car_distance` 给 ACC 控制器。

旧版模块 `front_car_detector.py` 使用绿色车身/图案检测，但当前主线 `vpa_acc.launch` 使用增强版 AprilTag 检测。
